# MongoDB Configuration
MONGODB_URI=mongodb://localhost:27017/
MONGODB_DB_NAME=video_editing_saas

# MongoDB Atlas (Production - update with your Atlas connection string)
# MONGODB_URI=mongodb+srv://username:password@cluster.mongodb.net/
# MONGODB_DB_NAME=video_editing_saas

# JWT Secret (Change this to a random secure string in production)
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Flask Configuration
FLASK_ENV=production
PORT=5001

# CORS Origins (Update with your Netlify URL after deployment)
CORS_ORIGINS=https://your-site.netlify.app,http://localhost:5173
