[build]
  command = "npm run build"
  publish = "dist"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200

[build.environment]
  NODE_VERSION = "18"

# Environment variables (update VITE_API_URL after Railway deployment)
# Add these in Netlify dashboard: Site settings → Environment variables
